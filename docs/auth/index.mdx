---
title: Auth & Access
description: Use one FastNear API key across the RPC and REST APIs, sent as a Bearer header or URL parameter.
slug: /auth
sidebar_position: 0
displayed_sidebar: rpcSidebar
page_actions:
  - markdown
---

# Auth & Access

One FastNear API key works across both the [RPC](/rpc) and the [REST APIs](/api). Log in at [dashboard.fastnear.com](https://dashboard.fastnear.com) to get a key, then send it on each request either way below.

Live example pages also support `Copy example URL` for sharing prefilled requests. Shared example URLs run automatically on load whenever they include operation state, and saved API keys and tokens are never included in those public docs URLs.

## Using the Authorization header

```bash
curl "https://rpc.mainnet.fastnear.com" \
  -H "Authorization: Bearer ${API_KEY}" \
  -H "Content-Type: application/json" \
  --data '{"method":"block","params":{"finality":"final"},"id":1,"jsonrpc":"2.0"}'
```

## Using the `?apiKey=` URL parameter

```bash
curl "https://rpc.mainnet.fastnear.com?apiKey=${API_KEY}" \
  -H "Content-Type: application/json" \
  --data '{"method":"block","params":{"finality":"final"},"id":1,"jsonrpc":"2.0"}'
```
