---
title: V1 Public Key Lookup
description: Resolve a full-access public key to indexed account IDs in the v1 namespace.
hide_table_of_contents: true
---

import FastnearDirectOperation from '@site/src/components/FastnearDirectOperation';

# V1 Public Key Lookup

Use the v1 endpoint for the newer namespace. It currently returns the same `{ public_key, account_ids }` shape as the v0 route.

<FastnearDirectOperation pageModelId="fastnear-v1-public-key-lookup" />
